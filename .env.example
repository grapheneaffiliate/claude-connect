# --- Core Settings ---
# 🔑 Optional: Secret key for authorizing MCP clients via the initialize method.
SECRET_KEY=my-very-secure-mcp-secret

# --- Search API Keys ---
# Required: Bing Search API v7 key
BING_API_KEY=PASTE_YOUR_BING_API_KEY_HERE
# 🌐 Optional: Google Custom Search API Key
GOOGLE_API_KEY=PASTE_YOUR_GOOGLE_API_KEY_HERE
# 🌐 Optional: Google Custom Search Engine ID (CX)
GOOGLE_CX=PASTE_YOUR_GOOGLE_CX_HERE

# --- HTTP Authentication (🔒 Optional) ---
# Required if using HTTP mode with JWT: Secret for encoding/decoding JWTs
JWT_SECRET_KEY=a-different-very-secure-jwt-secret
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
# Note: To obtain a JWT token, send a POST request to http://127.0.0.1:8000/token with credentials (default: username="testuser", password="testpassword"). Customize credentials in main.py if needed.

# --- Database (🚀 Optional - Defaults to SQLite) ---
# Example for PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=sqlite:///model_context.db

# --- Caching (🚀 Optional) ---
# Example for Redis:
# REDIS_URL=redis://localhost:6379/0

# --- Internationalization (🌎 Optional) ---
# Default language code (e.g., en, es, fr)
LANGUAGE=en
